#include <stdio.h>

int run = 1;

int main()
{
    int choice_of_struct = 0;
    printf("1. Create a structure");
    printf("2. Use a structure");
    switch (choice_of_struct)
    {
    case 1:
        while(run == 1)
        {
            
        }    
        break;
    case 2:
        printf("feature currently not available");
    default:
        break;
    }
    
    return 0;
}